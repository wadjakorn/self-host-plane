# Plane stack: generated by setup.sh, contains secrets (DB pw, SECRET_KEY, etc.)
plane-selfhost/plane-app/
plane-selfhost/*.log

# MCP server local config (use .env.example as template)
plane-mcp/.env

# General
*.log
.DS_Store
